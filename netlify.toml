[build]
  command = "npm install"
  functions = "netlify/functions"

[dev]
  command = "npx serve ."
  targetPort = 3000

[functions]
  directory = "netlify/functions"

[context.production]
  node_bundler = "esbuild"